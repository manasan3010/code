import {ConfirmModalType} from './utils';

export class ConfirmModalData {
  title: string;
  bodyText: string;
  confirmButton: boolean;
  confirmButtonText: string;
  modalType: ConfirmModalType;
}
